{
  "name": "tor-with-auth-tests",
  "private": true,
  "scripts": {
    "postinstall": "tests/goss/install.sh v0.3.5 node_modules/.bin",
    "build": "docker build --no-cache --force-rm --quiet -t tor-with-auth .",
    "goss": "GOSS_FILES_PATH=tests/goss GOSS_OPTS='--color -f rspecish' dgoss run tor-with-auth",
    "pretest": "npm run build && npm run goss",
    "test": "cucumber-js tests/features"
  },
  "dependencies": {
    "cucumber": "^4.2.1",
    "debug": "^3.1.0"
  }
}
